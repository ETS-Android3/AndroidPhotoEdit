<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="vm"
            type="com.yunianshu.library.ui.sticker.StickerViewModel" />

        <variable
            name="click"
            type="com.yunianshu.library.ui.sticker.StickerActivity.StickerClickProxy" />

        <variable
            name="adapter"
            type="com.yunianshu.library.adapter.StickerAdapter" />

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:fitsSystemWindows="true"
        tools:context=".ui.crop.FilterActivity">

        <ImageView
            android:id="@+id/ivBack"
            android:layout_width="wrap_content"
            android:layout_height="?android:attr/actionBarSize"
            android:clickable="true"
            android:contentDescription="@string/text_back"
            android:focusable="true"
            android:onClick="@{()->click.back()}"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_btn_back_dark" />

        <com.yunianshu.library.view.imagezoom.ImageViewTouch
            android:id="@+id/stickerImage"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_margin="20dp"
            loadBitmap="@{vm.bitmap}"
            app:layout_constraintBottom_toTopOf="@+id/stickerList"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/ivBack" />


        <com.yunianshu.library.view.StickerView
            android:id="@+id/stickerView"
            android:layout_width="0dp"
            android:layout_height="0dp"
            android:layout_gravity="center"
            app:layout_constraintBottom_toBottomOf="@+id/stickerImage"
            app:layout_constraintEnd_toEndOf="@+id/stickerImage"
            app:layout_constraintStart_toStartOf="@+id/stickerImage"
            app:layout_constraintTop_toTopOf="@+id/stickerImage" />

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/stickerList"
            android:layout_width="match_parent"
            android:layout_height="80dp"
            android:orientation="horizontal"
            submitList="@{vm.list}"
            adapter="@{adapter}"
            app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
            app:layout_constraintBottom_toTopOf="@+id/linearLayout"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent" />

        <LinearLayout
            android:id="@+id/linearLayout"
            android:layout_width="match_parent"
            android:layout_height="56dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginStart="20dp"
                android:background="@drawable/ic_close"
                android:onClick="@{()->click.cancel()}"
                android:padding="20dp" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:gravity="center"
                android:text="@string/text_sticker"
                android:textColor="#999999"
                android:textSize="16sp" />

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_marginEnd="20dp"
                android:background="@drawable/ic_yes"
                android:onClick="@{()->click.complete()}" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>